# Домен-модель и контракты системы ЛЁН

Версия 0.1 (скелет) · Зона: Ф · Статус: **не начато** (структура задана, содержимое — TODO)
Родитель: `ТЗ-1-система-ЛЁН-MVP.md` (разд. 4–5, 7). Конвенции: `AGENTS.md`.

Назначение: единый источник правды по сущностям, событиям и протоколам. Это вход для бэка (Ф), фронта (Д — типы API) и железа (И — протокол ESP32). Проектируется под полный продукт, реализуется срезами (принцип №1 AGENTS.md).

---

## 1. Сущности (TODO: атрибуты, связи, инварианты)
- [ ] **Изделие** (Product): модель, версии, профили, фото, эталоны первого слоя, допуски «к серии»/«к ночи»
- [ ] **СлайсАртефакт** (SliceArtifact): G-code + версия профиля + версия пост-процессинга
- [ ] **Задание** (Job): изделие × количество; поля priority/deadline заложены, логика — этап 2
- [ ] **ЭкземплярДетали** (PartInstance): полная цепочка трассируемости (ТЗ-1 п. 5.5)
- [ ] **Принтер** (Printer): режим (автономный/«Контроль»), состояние FSM, наработка
- [ ] **Катушка** (Spool): материал, цвет, партия, остаток (расчёт + калибровка весами), QR-id
- [ ] **ПоверхностьАдгезии** (BuildSurface) / **КлейМодуль** (GlueUnit): состав по итогам Э1 🔬
- [ ] **Инцидент** (Incident): кадры, вердикты каскада, решение VLM, исход от человека
- [ ] **ЗадачаОператора** (OperatorTask), **Смена/Отчёт** (ShiftReport)
- [ ] **Площадка** (Site), **Стойка** (Rack), **Пользователь/Роль**

## 2. Конечный автомат принтера (TODO: диаграмма + таблица переходов)
- [ ] Состояния: `idle → preflight → heating → printing → cooling → ejecting → sorting → confirm_drop → idle` + ошибочные ветки (`fault_*`)
- [ ] Какие события инициируют переходы (Moonraker, ESP32, CV, оператор, VLM)
- [ ] Усечённый автомат «Контроль»-принтера (по камере)
- [ ] Где исполняется: edge ✅; как центр наблюдает (проекция по событиям)

## 3. Карта MQTT-топиков (TODO)
- [ ] Схема имён: `len/{site}/{printer}/...` — телеметрия, команды, события механики, статусы edge
- [ ] QoS, retain, идемпотентность команд, формат payload (JSON-схемы)

## 4. Протокол edge ↔ ESP32 (механика) — совместно с И (TODO)
- [ ] Команды: сниматель (проход/стоп), заслонка (годен/брак), доводчик (открыть/закрыть), клей-модуль (нанести/промыть) 🔬
- [ ] События: концевики, датчик тока (порог усилия), ИК-барьер, весы (стабилизированное значение), уровень клея
- [ ] Тайминги цикла снятия, таймауты, поведение при потере связи с ESP32

## 5. HTTP API центра (TODO: OpenAPI)
- [ ] Изделия/задания/очередь; инциденты и подтверждения; отчёты; auth/роли
- [ ] Контракт `SliceService` (вход: модель+профиль → выход: артефакт)
- [ ] Контракт `Notifier` (канало-независимые уведомления)

## 6. Синхронизация edge ↔ центр (TODO)
- [ ] Журнал первичен на edge; асинхронная идемпотентная репликация; поведение по правилам деградации (ТЗ-1 разд. 4)
- [ ] Зеркало очереди + кэш G-code на 24–48 ч

## 7. События для датасета (TODO)
- [ ] Что и в каком формате сохраняем с каждого инцидента/чекпоинта/финала для дообучения (Э6)
