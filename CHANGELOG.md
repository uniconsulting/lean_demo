# Changelog проекта ЛЁН

Формат: дата · артефакт · что сделано · **статус** (`не начато / изучение / в разработке / нужен тест / готово ✓(тест)`).
Правила ведения — `AGENTS.md`. «Готово» — только после подтверждающего теста (авто или ручной с указанием, кто и что проверил).

---

## 2026-07-12 (Д) — Вертикальный ритм ночного hero и действия блокеров

| Артефакт | Изменение | Статус |
|---|---|---|
| Hero «Зарядить ночь» | Высота первого смыслового блока и всех четырёх KPI-карточек увеличена единым шагом. У «Готовности ночи» и «Покрытия очереди» появился дополнительный вертикальный воздух; подпись цели отделена от bar-chart, при этом утверждённая анатомия карточек и «Контур готовности» не менялись. | **нужен тест** (ручное визуальное ревью Д: desktop/mobile, light/dark) |
| «Стойка автономии», физические блокеры | Карточки «Тара годных заполнена» и «Катушка #17…» получили зарезервированную нижнюю action-зону; кнопки «Подтвердить освобождение» и «Заменить катушку» закреплены в правом нижнем углу и сохраняют позицию в закрытом состоянии. | **нужен тест** (ручное UX-ревью Д: исходное и закрытое состояние) |
| Auto + Browser QA + GitHub Pages | TypeScript и ESLint завершились без ошибок; локальные Vitest/Vite были заблокированы выгруженными macOS/iCloud `dataless`-файлами зависимостей и тестов. Финальная production-сборка и публичный Browser QA проверяются через Pages workflow после push. | **нужен тест** (требуется завершить Pages и визуальную проверку) |

### Незакрытые статусы (актуально)
- **Д**: проверить новый вертикальный ритм hero и положение CTA блокеров в обеих темах.
- **Д**: «Зарядить ночь» и «Очередь» остаются **нужен тест** до ручного подтверждения.
- **Д + Ф**: после утверждения ночного протокола перейти к библиотеке изделий.

## 2026-07-12 (Д) — Возврат ночного протокола к утверждённым UI-паттернам

| Артефакт | Изменение | Статус |
|---|---|---|
| Hero KPI «Готовность ночи» и «Покрытие очереди» | Обе карточки приведены к единой производственной KPI-анатомии. Покрытие переиспользует `DottedTime`, clock-label, icon-action, подпись цели и bar-chart из «Сводки смены»; нейтральная поверхность и `var(--ink)` корректно адаптируются к обеим темам. Готовность использует ту же геометрию: label → число → статус → 7-сегментная шкала. | **нужен тест** (ручное визуальное ревью Д: light/dark) |
| `PrinterStatusTile`, «Ферма», «Стойка автономии» | Утверждённая карточка принтера вынесена в общий domain-компонент: тот же line-art K1, status-dot, изделие, материал и FSM footer теперь используются обоими экранами. Дублирующие стили «Фермы» удалены без изменения её поведения. | **нужен тест** (регрессионное ревью Д: «Ферма» и ночной экран) |
| Контур готовности | Утверждённый пользователем блок сохранён без визуальной переработки; меняются только данные состояний. | **нужен тест** (ручное подтверждение в составе экрана) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 24 Vitest/RTL-теста, production build, `git diff --check` и scan отсутствия `border:` пройдены. Public Browser подтвердил одинаковую геометрию KPI (`132px`), light/dark token colors, `DottedTime` `18:40 → 15:20`, отсутствие overlay/console errors и единый CSS Module printer tile на `#/farm` и `#/night`. Playwright fallback использован только для screenshot из-за compositor-сбоя Browser; desktop `1536×1024` и dark mobile `390×844`, `scrollWidth = clientWidth = 390`. Printer line-art получил общий dark contrast filter. Pages run `29188893093` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное подтверждение Д) |

### Незакрытые статусы (актуально)
- **Д**: проверить новую общую геометрию hero KPI и единый printer-card в light/dark.
- **Д**: «Зарядить ночь» и «Очередь» остаются **нужен тест** до ручного подтверждения.
- **Д + Ф**: после утверждения ночного протокола перейти к библиотеке изделий.

## 2026-07-12 (Д) — Full-screen concept и React-экран «Зарядить ночь»

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/design-concepts/night-screen-v1.html`, `night-screen-v1.png` | Создана и визуально проверена full-screen спецификация `1536×1024`: крупная готовность ночи, линейный контур из 7 проверок, объектная карта 8 принтеров, два физических блокера, политика деградации и approval rail. Встроенный ImageGen вернул network error; без CLI/API fallback концепт собран локально как code-native артефакт и отрендерен в PNG. | **нужен тест** (ручное ревью Д: композиция ночного протокола) |
| `src/pages/night/`, `src/data/night.ts`, `src/app/router.tsx` | Placeholder `#/night` заменён интерактивным React-экраном. Работают подтверждение освобождения тары, замена катушки, автоматический пересчёт preflight/блокеров, исключение K1C-06 с пересчётом покрытия, создание mock-задачи и финальное подтверждение ночи. Реальные команды оборудованию не вызываются. | **нужен тест** (ручное UX/UI-ревью Д: desktop/mobile, light/dark и сценарии) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 24 Vitest/RTL-теста и production build пройдены. Публичный Browser QA подтвердил оба пути: закрытие тары + замена катушки → `8/8`, `0` блокеров и подтверждение; исключение K1C-06 → `7/7`, покрытие `15 ч 20 мин` и доступный approval. Исправлено выравнивание dotted-метрик, найденное на первом публичном рендере; добавлен project favicon вместо Pages `404`. Light/dark без horizontal overflow и framework overlay; mobile `390×844` имеет `scrollWidth = clientWidth = 390`. Concept и финальный implementation screenshot просмотрены через `view_image`; финальный Pages run `29188200399` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное подтверждение Д) |

### Незакрытые статусы (актуально)
- **Д**: проверить «Зарядить ночь» в light/dark и desktop/mobile, включая оба пути: закрыть физические блокеры и исключить K1C-06.
- **Д**: экран «Очередь» и его concept остаются **нужен тест** до ручного подтверждения.
- **Д + Ф**: после утверждения ночного протокола перейти к библиотеке изделий.

## 2026-07-12 (Д) — Drag-and-drop очереди и полноэкранная модалка

| Артефакт | Изменение | Статус |
|---|---|---|
| Порядок выполнения, `@dnd-kit/*` | Удалены отдельные стрелки вверх/вниз. Единственный `GripVertical` выровнен по центральной горизонтали строки и стал pointer/touch/keyboard drag-handle; при переносе активная строка приподнимается, соседние строки плавно занимают новые позиции, порядковые номера пересчитываются. | **нужен тест** (ручное UX-ревью Д: ощущение захвата и скорость перестроения) |
| Модалка «Создать серию» | Диалог перенесён через React portal в `document.body`: затемнение и blur теперь охватывают Sidebar, Header, рабочую область и context rail, а окно центрируется относительно viewport, а не центрального блока. | **нужен тест** (ручное визуальное ревью Д в light/dark) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 21 Vitest/RTL-тест и production build пройдены. Реальный pointer-drag на `1536×1024` переместил «Кронштейн-12» на первое место. Диалог центрирован в точках `768×512` и `195×422`; mobile `390×844` без horizontal overflow, старых стрелок и console errors. Pages run `29185506022` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное подтверждение Д) |

### Незакрытые статусы (актуально)
- **Д**: проверить плавность drag-reorder мышью и касанием, а также полноэкранный blur модалки в обеих темах.
- **Д**: экран «Очередь» и его full-screen concept остаются **нужен тест** до ручного подтверждения.
- **Д + Ф**: после утверждения «Очереди» перенести ночной протокол.

## 2026-07-12 (Д) — Утверждение K1C-04, full-screen concept и React-экран «Очередь»

| Артефакт | Изменение | Статус |
|---|---|---|
| Карточка принтера K1C-04 | Получено ручное подтверждение Д: «Можно переходить к следующему разделу». Закрыты full object-detail экран, плоское состояние `confirm_drop` и метрика `100 %`. | **готово ✓(ручной тест: Д, 2026-07-12, карточка принтера принята)** |
| `docs/design-concepts/queue-screen-v1.html`, `queue-screen-v1.png` | Создана и визуально проверена full-screen спецификация `1536×1024`: open queue-lane, покрытие ночи, explainability и right rail. Оба встроенных ImageGen-пути вернули network error, поэтому без CLI/API fallback concept собран локально как code-native артефакт и отрендерен в PNG. | **нужен тест** (ручное ревью Д: композиция «Очереди» и соответствие утверждённой системе) |
| `src/pages/queue/`, `src/data/queue.ts`, `src/app/router.tsx` | Placeholder `#/queue` заменён React-экраном: coverage `18 ч 40 мин`, упорядоченный lane из четырёх серий, выбранная и заблокированная строки, причины порядка, blocker action и контекст выбранной серии. Работают выбор, reorder, quantity, night inclusion, создание mock-черновика и защищённая отмена. | **нужен тест** (ручное UX/UI-ревью Д: desktop/mobile, light/dark и интерактивные состояния) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 20 Vitest/RTL-тестов и production build пройдены. Browser QA проверил native-size `1536×1024`, выбор блокера, reorder, создание черновика и confirm-dialog отмены; dark mobile `390×844` без общего horizontal overflow, framework overlay и console errors. Concept и финальный browser screenshot просмотрены через `view_image`; материальных расхождений не осталось. Pages run `29184918763` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное подтверждение Д) |

### Незакрытые статусы (актуально)
- **Д**: проверить «Очередь» в light/dark и desktop/mobile; до подтверждения экран и concept остаются **нужен тест**.
- **Д + Ф**: после утверждения «Очереди» перенести ночной протокол.
- **Ф**: сверить команды reorder/cancel/assign и edge mirror с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Оптика roadmap и метрики прогресса K1C-04

| Артефакт | Изменение | Статус |
|---|---|---|
| FSM roadmap, этап `confirm_drop` | Объёмный action-gradient на малой поверхности заменён плоским `warning-soft` состоянием с тонким внутренним акцентом. Семантика ожидания сохранена без конкуренции с крупной карточкой «Следующее действие». | **нужен тест** (ручное визуальное ревью Д в light/dark) |
| Телеметрия, `Прогресс` | Значение разделено на dotted-число `100` и отдельный компактный знак `%`; уменьшен отрицательный tracking локального display-value. Метрика снова сканируется как процент, не слипаясь в декоративный блок. | **нужен тест** (ручное визуальное ревью Д: считываемость `100 %`) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 16 Vitest/RTL-тестов и production build пройдены. Browser QA на `1536×1021` в light/dark и `390×844` подтвердил плоский active-stage, читаемый процент, отсутствие horizontal overflow и console errors. Pages run `29183389221` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное подтверждение Д) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить плоское состояние `confirm_drop` и считываемость `100 %` в обеих темах.
- **Д**: пройти карточку K1C-04 целиком; экран сохраняет статус **нужен тест**.
- **Д + Ф**: после утверждения карточки принтера перенести очередь и ночной протокол.

## 2026-07-12 (Д) — Утверждение «Фермы» и React-карточка принтера

| Артефакт | Изменение | Статус |
|---|---|---|
| Экран «Ферма» | Получено ручное подтверждение Д: «Можем переходить к следующему разделу». Закрыты геометрия стоек, printer-card alignment и camera-frame в контекстной панели. | **готово ✓(ручной тест: Д, 2026-07-12, экран «Ферма» принят)** |
| `src/pages/printer-detail/`, `src/data/printerDetail.ts`, `src/app/router.tsx` | Placeholder `#/printers/:printerId` заменён object-detail экраном K1C-04: крупный camera-frame, FSM-лента, телеметрия, текущее задание, катушка, поверхность/клей, edge-sync, события и action rail. Реализованы mock-состояния live-камеры, подтверждения тары, замены катушки, создания задачи и защищённой паузы. | **нужен тест** (ручное UX/UI-ревью Д: desktop/mobile, light/dark и интерактивные состояния) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 16 Vitest/RTL-тестов и production build пройдены. В Browser проверены `#/printers/K1C-04`, live-камера, завершение физического действия и обязательный confirm-dialog для паузы. Light desktop `1536×1021` и dark mobile `390×844` без horizontal overflow, framework overlay и console errors. Pages run `29182944951` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное визуальное и сценарное ревью Д) |

### Незакрытые статусы (актуально)
- **Д**: пройти карточку K1C-04 в light/dark на desktop/mobile, проверить визуальный приоритет камеры, FSM и action rail.
- **Д + Ф**: после утверждения карточки принтера перенести очередь и ночной протокол.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Коррекция карты стоек и camera-frame «Фермы»

| Артефакт | Изменение | Статус |
|---|---|---|
| Rack A/B, printer cards | Удалены четыре декоративные направляющие, воспринимавшиеся как scroll-indicators. Rack-поверхности приведены к чистой геометрии Control-only. Line-art принтера уменьшен и выровнен по верхнему левому информационному блоку. | **нужен тест** (ручное визуальное ревью Д) |
| `public/assets/farm-print-camera-v1.webp`, context rail | Через ImageGen создан оригинальный grayscale-кадр активной FDM-печати без UI-текста, брендов и сторонних изображений. PNG `1,9 МБ` оптимизирован в WebP `112 КБ` и подключён к last/live frame. | **нужен тест** (ручное визуальное ревью Д) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 12 Vitest/RTL-тестов и production build пройдены. На `1536×1021` псевдоэлементы Rack A/B и rail отсутствуют (`content: none`), padding равен `9px`; line-art и text-column начинаются на одной Y-координате, camera WebP загружен `1448×1086` с `object-fit: cover`. Light/dark, live-состояние и mobile `390×844` без horizontal overflow, overlay и console errors. Pages run `29182349453` успешен. | **нужен тест** (автопроверка пройдена; нужно ручное ревью Д) |

### Незакрытые статусы (актуально)
- **Д**: проверить чистую геометрию Rack A/B, верхнее выравнивание line-art и реалистичность camera-frame в light/dark.
- **Д + Ф**: после утверждения «Фермы» перенести карточку принтера, очередь и ночной протокол.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Ручное утверждение golden screen и React-экран «Ферма»

| Артефакт | Изменение | Статус |
|---|---|---|
| Golden screen «Сводка смены», UI-дизайн-система | Получено ручное подтверждение Д: «Все окей, можно переходить к работе над следующими разделами». Закрыты визуальный gate, последний пакет KPI/action rail и обе темы. | **готово ✓(ручной тест: Д, 2026-07-12, golden screen принят)** |
| `docs/design-concepts/farm-screen-v1.png` | Создана оригинальная full-screen визуальная спецификация экрана «Ферма» в утверждённой системе; сторонние изображения не использованы. | **нужен тест** (ручное ревью композиции «Фермы») |
| `src/pages/farm/`, `src/data/farm.ts`, `src/app/router.tsx` | Legacy-placeholder маршрута `#/farm` заменён полноценным React-экраном: Rack A/B, Control-only, выбор принтера, фильтры, live-кадр, FSM/источник, события и ручная отметка занятости. Оболочка не перемонтируется; title браузера следует за маршрутом. | **нужен тест** (ручное UX/UI-ревью Д) |
| Auto + Browser QA + GitHub Pages | TypeScript, ESLint, 12 Vitest/RTL-тестов и production build пройдены. В Browser проверены `#/farm`, выбор K1C-01, фильтр блокеров с возвратом к K1C-04, live-кадр, Control-only и ручная смена занятости. Light/dark на `1536×1021`, mobile `390×844`; horizontal overflow, framework overlay и console errors отсутствуют. Pages run `29181832130` успешен. | **нужен тест** (автопроверка пройдена; требуется ручное визуальное и сценарное ревью Д) |

### Незакрытые статусы (актуально)
- **Д**: пройти экран «Ферма» в light/dark и desktop/mobile; до ручного подтверждения новый экран остаётся **нужен тест**.
- **Д + Ф**: после утверждения «Фермы» перенести карточку принтера, очередь и ночной протокол.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Семантические строки action rail и оптическое выравнивание KPI

| Артефакт | Изменение | Статус |
|---|---|---|
| `Следующее действие`, `Действие закрыто`, `Требует внимания` | Описания разделены на две явные смысловые строки. CTA обоих состояний action-карточки закреплён у нижнего внутреннего края, чтобы переключение состояния не меняло геометрию. | **нужен тест** (ручное визуальное ревью Д) |
| `Покрытие очереди` | Icon-action `32×32` вынесен из layout-flow заголовка и больше не растягивает первую строку. Верхний inset названия приведён к двум утверждённым KPI-карточкам. | **нужен тест** (ручное визуальное ревью Д) |
| Browser QA и GitHub Pages | На `1536×1055` все три KPI имеют одинаковые `316,12×196`, label inset `17px` и header height `15px`. Оба состояния action-карточки сохраняют CTA в `18px` от нижнего края; все три описания отрисованы двумя строками. Light/dark и `390×844` без overflow, framework overlay и console errors. Pages run `29181048108` успешен. | **нужен тест** (автопроверка пройдена; требуется человеческое подтверждение) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить двухстрочные описания обоих состояний action-карточки, incident-карточки и единый верхний ритм трёх KPI-карточек.
- **Д + Ф**: утвердить golden screen целиком перед переносом следующих разделов.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Коррекция KPI после визуального ревью

| Артефакт | Изменение | Статус |
|---|---|---|
| KPI-поверхности, `DottedTime` | Удалён ошибочно добавленный фиксированный уровень высоты заголовка, создававший лишний верхний отступ. Все три графика возвращены к единому утверждённому rounded-bar дизайну и различаются только математическими последовательностями высот. Между `13`, `:` и `20` добавлен явный оптический интервал; двоеточие получило собственную ширину и центрирование. | **нужен тест** (ручное визуальное ревью Д: верхний ритм, три разных набора показаний, считываемость `13:20`) |
| Browser QA и GitHub Pages | На `1536×1055` измерены одинаковые карточки `316×196`, верхний inset `17px`, интервалы вокруг двоеточия `6,3px`; подтверждены три различных набора высот одного bar-компонента. `390×844` без overflow, console errors отсутствуют. Pages run `29180597759` успешен. | **нужен тест** (автопроверка пройдена; требуется человеческое подтверждение исправления) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить исправленные KPI-карточки на golden screen; до этого статус остаётся **нужен тест**.
- **Д + Ф**: утвердить golden screen целиком перед переносом следующих разделов.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-12 (Д) — Геометрия header, KPI и action rail

| Артефакт | Изменение | Статус |
|---|---|---|
| `AppShell`, `LiveDot` | Правая часть header перестроена справа налево: две кнопки `38×38` с равным внешним inset, профиль, имя, вертикальный разделитель и русифицированный статус `Edge в сети`. Halo вокруг status-dot удалён; два активных статуса используют единый recording-индикатор с циклической opacity-анимацией. | **нужен тест** (ручное визуальное ревью Д: геометрия desktop и считываемость recording-состояния) |
| `tokens.css`, общие surfaces | В светлой теме снижена интенсивность shell/soft/float shadows и внутренних технических линий. Ночная тема сохранена более контрастной. | **нужен тест** (ручное ревью Д: сравнить глубину главных фреймов со студийными референсами) |
| KPI-поверхности, `DottedTime` | `Выпуск`, `Первый проход` и `Покрытие очереди` приведены к равной сетке и высоте. Текстовая ссылка очереди заменена icon-only действием в заголовке; добавлены три разных графика — bars, trend line и segmented coverage. Двоеточие времени центрируется отдельным компонентом. | **нужен тест** (ручное ревью Д: геометрия, оптика `13:20`, различимость графиков) |
| `Следующее действие` | Уменьшена минимальная высота, сокращены вертикальные интервалы, CTA получил компактную ширину. Для ночной темы action-gradient стал непрозрачным и контрастным, основной текст переключается на светлый. | **нужен тест** (ручное ревью Д: вертикальный ритм и контраст light/dark) |
| Browser QA и GitHub Pages | На `1536×1055` подтверждены header-buttons `38×38`, одинаковые KPI `316×196`, action-card `233px`, recording opacity и рабочий CTA; на `390×844` нет overflow. Light/dark без console errors. Pages run `29180331332` успешен. | **нужен тест** (автопроверка пройдена; требуется человеческое визуальное подтверждение пакета) |

### Незакрытые статусы (актуально)
- **Д + Ф**: утвердить golden screen после пакета геометрических правок; до подтверждения пакет остаётся **нужен тест**.
- **Д**: проверить header, три KPI и action rail в обеих темах на desktop и mobile.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-11 (Д) — Переработка градиентов signal-поверхностей

| Артефакт | Изменение | Статус |
|---|---|---|
| `src/styles/tokens.css`, `FarmObject` | Круговые radial-блики удалены из четырёх семантических градиентов и карты фермы. Stable/action/info/danger переведены на широкие многотональные диагональные поля; для ночной темы добавлены отдельные приглушённые graphite-варианты без яркого hotspot. | **нужен тест** (ручное визуальное ревью Д: сравнить signal-поверхности с приложенными референсами в light/dark) |
| `docs/UI-дизайн-система-панели.md` | Gradient contract дополнен правилом: рассеянное цветовое поле без локальной светящейся точки. | **нужен тест** (подтвердить вместе с golden screen) |

### Незакрытые статусы (актуально)
- **Д + Ф**: утвердить golden screen и новый характер градиентов в обеих темах; до этого пакет остаётся **нужен тест**.
- **Д**: проверить, что signal-поверхности читаются как семантика, а не декоративный световой эффект.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.

## 2026-07-11 (Д) — React golden screen и expressive industrial design system

| Артефакт | Изменение | Статус |
|---|---|---|
| `src/`, `index.html`, Vite/TypeScript/ESLint/Vitest config | Создан React 19 + Vite + TypeScript strict shell с `HashRouter`: постоянные Sidebar, CommandBar, RouteStage, ContextRail и ThemeProvider; 13 маршрутов; typed mock-данные; Lucide React; локальные Manrope и Doto. Старый экран удаляется при смене маршрута, анимация применяется только к новому содержимому за 120 ms. | **нужен тест** (авто: typecheck, ESLint, 6 Vitest/RTL-тестов и production build пройдены; нужен ручной UX-review) |
| `src/pages/shift-summary/`, `src/components/domain/FarmObject*` | Реализован golden screen «Сводка смены»: крупная объектная карта фермы, состояния `22 / 4 / 3 / 1`, выпуск/очередь/качество, action и incident surfaces, журнал и готовность ночи. Работают выбор принтера, фильтр блокеров, выполнение/отмена действия и две темы. | **нужен тест** (ручное ревью Д + Ф: композиция, считываемость, действия, обе темы, desktop/tablet/mobile) |
| `docs/UI-дизайн-система-панели.md`, `docs/UX-видение-панели.md`, `README.md` | Зафиксированы выразительное industrial enterprise-направление, токены, компоненты, motion, accessibility, объектный фокус и сопоставление паттернов со всеми 13 экранами. Сторонние изображения не добавлялись; сохранена только публичная ссылка и текстовое описание принципов. | **нужен тест** (ручное ревью Д: утвердить дизайн-контракт до переноса остальных экранов) |
| `public/legacy/`, redirect-stubs 01–13 | Прежние HTML-прототипы сохранены как временный fallback; прямые старые URL перенаправляют в соответствующие hash-маршруты React SPA. Остальные 12 новых маршрутов пока показывают связанный архитектурный каркас и ссылку на legacy-экран. | **нужен тест** (авто: маршруты и redirects покрыты тестами; ручной сценарный проход после публикации) |
| `public/assets/` | Подготовлены три оригинальные схемы без UI-текста: стойка фермы, принтер K1-класса и выбранное изделие. Assets предназначены для object-focus композиций следующих экранов и не копируют сторонние изображения. | **нужен тест** (ручное визуальное ревью при подключении к соответствующим экранам) |
| `.github/workflows/pages.yml` | Pages workflow переведён с публикации исходной папки на проверку React-проекта и deployment production-сборки `dist`. | готово ✓(авто: GitHub Actions run `29160528698`; публичные `index.html`, SVG asset и legacy redirect отвечают HTTP 200, 2026-07-11) |

### Незакрытые статусы (актуально)
- **Д + Ф**: вручную утвердить golden screen в светлой и ночной теме; до этого 12 следующих экранов не переносятся и весь новый пакет остаётся **нужен тест**.
- **Д**: проверить desktop, tablet и `390×844`, понятность Lucide-иконок, display-метрик и семантических signal-поверхностей.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.
- **И**: ревью ТЗ-2, `hardware/` и справочника по своей зоне.

## 2026-07-11 (Д) — Переходы без моргания и перезагрузки оболочки

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/prototypes/prototype.js` | Удален переход `opacity → 90 ms → location.assign()`, который гасил весь документ и заново создавал меню, тему и SVG. Добавлена фоновая загрузка следующего HTML с кэшем, атомарная замена только `.workbench`, History API и поддержка Back/Forward; при ошибке остается обычная навигация. | **нужен тест** (ручной проход опубликованного демо Д: оценить субъективную плавность частых переходов) |
| `docs/prototypes/prototype.css` | Отключены cross-document View Transitions и исчезновение всего `body`; анимация ограничена `commandbar` и `screen-layout`, sidebar остается стабильным. Сохранен `prefers-reduced-motion`. | **нужен тест** (ручное визуальное ревью dark/light в целевых браузерах) |
| Browser QA | На локальном HTTP проверены `Сводка → Ферма → Back`, dark/light, desktop и `390×844`: URL, title, h1 и активный раздел обновляются; 10 иконок сохраняются; opacity остается `1`; overflow и console errors отсутствуют. Серверный лог при проходе разделов получает только целевые HTML — CSS, JS и Lucide повторно не загружаются. | **нужен тест** (автотест пройден; требуется человеческое подтверждение устранения моргания) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить на GitHub Pages, что смена разделов больше не воспринимается как перезагрузка.
- **Д**: подтвердить набор иконок, светлую семантическую палитру и интенсивность теней/линий.
- **Д + Ф**: пройти 13 экранов и подтвердить продуктовую связку разделов.

## 2026-07-11 (Д) — Публикация демо через GitHub Pages

| Артефакт | Изменение | Статус |
|---|---|---|
| `.github/workflows/pages.yml` | Добавлен GitHub Actions workflow, публикующий содержимое `docs/prototypes` на GitHub Pages при изменениях в `main` и по ручному запуску. Деплой получает только необходимые права `contents: read`, `pages: write`, `id-token: write`. | готово ✓(авто: GitHub Actions run `29157018498`, job `deploy`, 2026-07-11) |
| GitHub `uniconsulting/lean_demo` | Проект опубликован в отдельном публичном GitHub-репозитории с сохранением существующего GitLab `origin`; GitHub подключен отдельным remote `github`. Pages включен в режиме `workflow`, HTTPS принудителен. | готово ✓(авто: push `3e5df66`, GitHub Pages API и HTTP 200 для стартового экрана и локального Lucide, 2026-07-11) |

### Незакрытые статусы (актуально)
- **Д**: визуально проверить публичную страницу `lean_demo` в целевых браузерах; инфраструктурный deployment подтвержден автоматически.
- **Д**: подтвердить набор иконок, новую светлую семантическую палитру и интенсивность теней/линий.
- **Д + Ф**: пройти 13 экранов через общую навигацию и подтвердить продуктовую связку разделов.

## 2026-07-11 (Д) — Иконки, стартовый маршрут и стабилизация переходов

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/prototypes/vendor/lucide/` | Локально подключена библиотека Lucide `1.24.0` и сохранена ее лицензия. Выбраны релевантные производственные и системные метафоры; прототипы не зависят от CDN. | **нужен тест** (ручное ревью Д: понятность иконок без подписей и соответствие разделам) |
| `prototype.js`, `prototype-init.js`, HTML-экраны 01–13 | Нумерация левой навигации заменена иконками; Lucide также заменяет заглушки темы, файла, камеры, чек-листов, предупреждений и действий. Тема применяется до CSS, меню имеет стабильную геометрию, переходы получают fade fallback и View Transitions при поддержке браузером. | **нужен тест** (ручной проход разделов в целевых браузерах) |
| `docs/prototypes/index.html` | Страница-карта `#flow` удалена; входная точка сразу открывает `01-shift-summary.html` через `location.replace` с meta-refresh fallback. | **нужен тест** (ручная проверка открытия папки/локального сервера) |
| `prototype.css` | Ослаблены тени и технические линии основных фреймов светлой темы; семантические цвета заменены на более сдержанные профессиональные оттенки: зеленый `#2c7447`, информационный `#48679d`, янтарный `#8e5a16`, красный `#a64046`. Контраст семантического текста на своих поверхностях доведен примерно до `5:1`. | **нужен тест** (визуальное ревью светлой темы Д) |
| Browser QA | Проверены редирект на сводку, локальная загрузка Lucide, 10 навигационных иконок и один активный раздел на каждом экране, dark/light, 26 комбинаций screen × desktop/mobile, отсутствие overflow и ошибок консоли. В ходе QA устранен риск зависания входящего fade в браузерах с частичной поддержкой transitions. | **нужен тест** (автотест пройден; требуется человеческое визуальное подтверждение) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить набор иконок, новую светлую семантическую палитру и интенсивность теней/линий.
- **Д + Ф**: пройти 13 экранов через общую навигацию и подтвердить продуктовую связку разделов.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.
- **И**: ревью ТЗ-2, `hardware/` и справочника по своей зоне.

## 2026-07-11 (Д) — Выбрана операторская геометрия и две нейтральные темы

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/prototypes/references/shift-summary-selected-reference.png` | Сохранен выбранный пользователем визуальный референс геометрии и компоновки стартового экрана. Референс используется как источник структуры, но его темно-синий базовый тон не переносится. | **нужен тест** (ручное визуальное ревью Д: подтвердить степень соответствия выбранному макету) |
| `docs/prototypes/prototype.css`, `prototype.js`, `01-shift-summary.html` | Сводка смены перестроена по выбранной операторской сетке: узкая навигация, KPI-полоса, карта фермы + состояния, три производственных показателя, журнал и правый action rail. Добавлены общие темы: ночная на нейтральных темно-серых оттенках и светлая на белых/холодных светло-серых; семантические цвета изолированы от базовых поверхностей. | **нужен тест** (ручное ревью Д + Ф: геометрия, плотность, семантика статусов и обе темы) |
| HTML-экраны 01–13, `prototype.js` | На все связанные экраны добавлен единый переключатель dark/light с сохранением выбора; навигация унифицирована до 10 разделов и корректно подсвечивает вложенные маршруты onboarding, слайсера и ночного протокола. | **нужен тест** (ручной сценарный проход всех разделов) |
| Browser QA | Проверены обе темы, сохранение темы при переходе, единая навигация, 28 комбинаций screen × desktop/mobile, отсутствие horizontal overflow и ошибок консоли. | **нужен тест** (автопроверка пройдена; требуется человеческое визуальное подтверждение) |

### Незакрытые статусы (актуально)
- **Д**: подтвердить выбранную геометрию на `01-shift-summary.html` в ночной и светлой теме либо дать точечные правки перед переносом плотной компоновки на остальные рабочие области.
- **Д + Ф**: пройти связанные маршруты 01–13 и подтвердить, что единая навигация соответствует продуктовой карте.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.
- **И**: ревью ТЗ-2, `hardware/` и справочника по своей зоне.

## 2026-07-11 (Д) — Полная пересборка UI/UX low-fi прототипов

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/prototypes/index.html`, `prototype.css`, HTML-экраны 01–13 | Пакет прототипов пересобран в единую grayscale UI/UX-систему: темная навигационная rail, командная строка, центральный операционный кадр, контекстный rail; общая модель «статус → причина/источник → следующее действие → аудит». Добавлены экраны карточки принтера, библиотеки и onboarding изделия, слайсер-потока, инцидентов, задач, журнала деталей, отчета смены, ролей и onboarding; существующие 01–04 приведены к той же системе и связаны переходами. | **нужен тест** (ручное ревью Д + Ф: пройти маршруты из `docs/UX-видение-панели.md`, оценить порядок действий и уместность визуальной системы) |
| `docs/prototypes/README.md`, сценарные карты 01–13 | README расширен до карты полного пакета и чек-листа ручного ревью; добавлены сценарные карты для экранов 05–13, состояния первых четырех карт синхронизированы с HTML. | **нужен тест** (ручное ревью Д + Ф: сверить HTML с UX-ТЗ и будущей доменной моделью, не фиксируя API) |
| Визуальная проверка статических HTML | Пройдены desktop/mobile проверки 14 экранов (28 комбинаций): по одному `h1`, общий stylesheet, отсутствие горизонтального overflow и ошибок браузерной консоли; проверен переход `index → Инциденты`. Исправлено расширение мобильного canvas у таблиц библиотеки и журнала. | **нужен тест** (автопроверка пройдена, нужен визуальный и сценарный human review) |

### Незакрытые статусы (актуально)
- **Д + Ф**: открыть `docs/prototypes/index.html`, пройти девять маршрутов из UX-ТЗ и подтвердить либо скорректировать новую UI/UX-систему экранов 01–13. До этого весь пакет прототипов остается **нужен тест**.
- **Ф**: сверить backend handoff UX-ТЗ с `docs/домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.
- **И**: ревью ТЗ-2 (критерии Э1/Э2, состав комплектов, график), структуры `hardware/`, справочника (блоки 3, 6).
- **Команда**: план этапов — подтвердить на синхроне; проверить чтение `AGENTS.md` из GPT (Д) и Gemini (И).
- Открытые вопросы ❓ ТЗ-1 — закрывает аудит (веха 0): состав парка, модель стойки, CF, камеры фермы, размещение центра.

## 2026-07-06 (Д) — UX-ТЗ low-fi прототипа панели

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/prototypes/index.html`, `prototype.css`, экраны 02–04 | Добавлены следующие low-fi прототипы в утвержденной surface-based системе: "Карта парка", "Очередь серий", "Зарядить ночь"; вынесен общий CSS с `R shell = 32px` и `R2 = R1 - I`; добавлен индекс для быстрого открытия HTML-экранов | **нужен тест** (ревью Д: проверить общий формат и выбрать, какие экраны детализировать следующими) |
| `docs/prototypes/README.md`, `docs/prototypes/01-сводка-смены.md`, `docs/prototypes/01-shift-summary.html` | Создан тестовый формат прототипа для экрана "Сводка смены": общий стандарт low-fi геометрии, markdown-описание для Canva + standalone grayscale HTML-макет с surface-based фреймами без border и радиусами по правилу `R2 = R1 - I` | **нужен тест** (ревью Д: утвердить формат макета перед размножением на остальные экраны) |
| `docs/UX-видение-панели.md` | Заглушка заменена содержательным UX-ТЗ для low-fi прототипа: производственный цикл `изделие → слайсинг → серия → очередь → печать → контроль → снятие → сортировка → журнал → отчёт`, день оператора, карта MVP-экранов, screen cards, Nielsen/Lean-принципы, уведомления, backend handoff и сценарии проверки | **нужен тест** (ручное ревью Д+Ф: экранные потребности, сценарии и соответствие будущей доменной модели/API) |

### Незакрытые статусы (актуально)
- **Д**: проверить пакет `docs/prototypes/index.html` и утвердить surface-based формат экранов 01–04 перед размножением на остальные разделы.
- **Ф**: сверить backend handoff UX-ТЗ с `домен-модель-и-контракты.md`, не фиксируя endpoint-ы преждевременно.
- **И**: ревью ТЗ-2 (критерии Э1/Э2, состав комплектов, график), структуры `hardware/`, справочника (блоки 3, 6).
- **Команда**: план этапов — подтвердить на синхроне; проверить чтение `AGENTS.md` из GPT (Д) и Gemini (И).
- Открытые вопросы ❓ ТЗ-1 — закрывает аудит (веха 0): состав парка, модель стойки, CF, камеры фермы, размещение центра.

---

## 2026-07-06 (вечер) — Ревью Ф пройдено, передача Д

| Артефакт | Изменение | Статус |
|---|---|---|
| Документация v2 целиком | Ревью основателя (Ф) пройдено, правок нет | готово ✓(ручной тест: ревью Ф, 2026-07-06) |
| Передача фронту (Д) | Д начинает с: `AGENTS.md` → справочник (блоки 1, 2, 5) → ТЗ-1 (особенно п. 5.6 панель и 5.10 слайсер) → скелет `docs/UX-видение-панели.md` — это его первый артефакт | **не начато** (зона Д, статус на момент записи) |

### Незакрытые статусы (на момент записи)
- **Д**: ревью п. 5.6 ТЗ-1 + заполнение `UX-видение-панели.md` (день оператора, карта экранов, дизайн-система).
- **И**: ревью ТЗ-2 (критерии Э1/Э2, состав комплектов, график), структуры `hardware/`, справочника (блоки 3, 6).
- **Команда**: план этапов — подтвердить на синхроне; проверить чтение `AGENTS.md` из GPT (Д) и Gemini (И).
- Открытые вопросы ❓ ТЗ-1 — закрывает аудит (веха 0): состав парка, модель стойки, CF, камеры фермы, размещение центра.

---

## 2026-07-06 (продолжение) — Справочник, папка инженера, чистка

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/справочник/` (8 файлов) | Создан справочник по 3D-печати: 00-оглавление, 01-общие понятия, 02-софт и архитектура, 03-железо, 04-сеть, 05-материалы, 06-регламенты и обслуживание, 07-ПРО-уровень ферм | **нужен тест** (вычитка командой: И — блоки 3/6, Ф — блоки 2/4/7; фактические цифры сверить с практикой стенда) |
| `hardware/` | Создана рабочая зона инженера: cad, drawings, firmware-esp32, klipper, datasheets, experiments, **integration** (контракт железо↔софт), notes + README с правилами | **нужен тест** (ревью И: устраивает ли структура) |
| Письмо-запрос ФлайТех | Убрано из репо → `..\lean-3d-business\` (прочитано, требования оттрассированы в ТЗ-1 прил. А) | готово ✓(листинг) |
| `AGENTS.md`, `README.md`, ТЗ-1 | Обновлены ссылки: справочник в порядке чтения, hardware/ в структуре, письмо — вне репо | готово ✓(правки внесены) |

## 2026-07-06 — Ревизия документации v2 (поэтапный разбор Ф + Claude)

| Артефакт | Изменение | Статус |
|---|---|---|
| `docs/ТЗ-1-система-ЛЁН-MVP.md` | Переписано v1→v2: Creality-only MVP; KPI пилота/стенда разделены; ветка клей vs PEI; edge = Linux x86; автомат+диспетчер на edge; слайсер-ядро и библиотека изделий в MVP; контур первого слоя (чекпоинт); стек порезан (без Redis/NATS); маркировка ✅/🔬/❓; деньги удалены | **нужен тест** (ревью Ф, затем Д и И по своим зонам) |
| `docs/ТЗ-2-прототип-стенд-и-стойка-автономии.md` | Переписано v1→v2: Э1 = сравнение клей-автомат vs PEI с критериями; KPI стенда С1–С5 (перенесены из ТЗ-1); контрольные точки 50/100/200 циклов; доводчик двери = стандартный узел; клей-модуль в составе стенда; составы комплектов без цен; ESP32 вместо «edge-GPIO» | **нужен тест** (ревью И — критерии Э1/Э2 и состав стенда; Ф — софт стенда) |
| `docs/план-этапов-проекта.md` | Переписано v1→v2: деньги и бизнес-дорожка удалены; софт-дорожка перестроена (слайсер-ядро и чекпоинт вместо адаптера Bambu); аудит дополнен (камеры, CF, версии прошивок); этап 3 сокращён до направлений | **нужен тест** (ревью команды на синхроне) |
| `AGENTS.md` | Создан: единый свод правил для людей и AI (статусы, маркировка, принципы, глоссарий) | **нужен тест** (проверить чтение из Claude Code, GPT/Codex, Gemini) |
| `CLAUDE.md`, `GEMINI.md` | Созданы как указатели на AGENTS.md | **нужен тест** (вместе с AGENTS.md) |
| `README.md` | Шаблон GitLab заменён входной страницей проекта | **нужен тест** (ревью Ф) |
| `CHANGELOG.md` | Создан, первая запись | готово ✓(ручной тест: сам файл) |
| Финансовые документы | Вынесены из репо в `..\lean-3d-business\` (экономика v2, финмодель, питч, устаревший техдок июня) | готово ✓(ручной тест: перемещение проверено листингом папок) |
| `docs/Pismo-zapros_Flai_774_Tekh.pdf` | Перенесено из корня в docs/ как источник требований | готово ✓(листинг) |
| `docs/домен-модель-и-контракты.md` | Создан скелет (зона Ф) | **не начато** (скелет) |
| `docs/UX-видение-панели.md` | Создан скелет (зона Д) | **не начато** (скелет) |

### Незакрытые статусы (напоминание)
- Все три основных документа ждут ревью: Ф — ТЗ-1 целиком; И — ТЗ-2 (критерии Э1/Э2, состав комплектов, график); Д — раздел 5.6 ТЗ-1 и скелет UX-видения.
- Открытые вопросы ❓ из ТЗ-1, закрываемые аудитом (веха 0): состав парка, модель стойки, CF-материалы, камеры фермы (IP/PoE vs прокси), размещение центра.
- `hardware/` ещё не создана — появится с первыми чертежами стенда.
